# 클라우드 배포 런타임 (app.py: 모드①②③ 모두 ensemble_pred.npz 만 사용)
streamlit>=1.39
numpy>=1.24
matplotlib>=3.7

# (선택) 모드③를 진짜 5시드 라이브 추론으로 업그레이드할 때만 필요 — 배포엔 불필요:
#   torch>=2.0
#   scikit-learn>=1.3
# 패널 QR 생성용(로컬에서만):
#   qrcode[pil]>=7.4
